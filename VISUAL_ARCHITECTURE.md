# 🌾 Water Dashboard - Visual Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER DEVICES                              │
│                    (Web Browser/Mobile)                          │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP/HTTPS
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                            │
│                    http://localhost:5173                        │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  React Router                                            │   │
│  │  ├─ /login, /signup (Public)                            │   │
│  │  ├─ /farmer/* (Protected - Farmer role)                 │   │
│  │  └─ /admin/* (Protected - Admin role)                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Redux Store                                             │   │
│  │  ├─ auth (token, user, isAuthenticated)                │   │
│  │  ├─ user (profile, users list)                         │   │
│  │  └─ waterUsage (logs, analytics)                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Axios Instance + Interceptors                          │   │
│  │  ├─ Request: Add Authorization header                  │   │
│  │  └─ Response: Handle 401 (logout)                      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  Components:                                                      │
│  ├─ Header (user info, logout)                                  │
│  ├─ Sidebar (role-based navigation)                             │
│  ├─ Pages (Dashboard, WaterLog, Analytics, Profile)             │
│  └─ UI Components (Card, Alert, Form, Button)                  │
└────────────────────────────┬────────────────────────────────────┘
                             │ REST API
                             │ /api/auth/*
                             │ /api/users/*
                             │ /api/water-usage/*
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND (Express)                           │
│                    http://localhost:5000                        │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Express App                                             │   │
│  │  ├─ Middleware: cors, morgan, json                      │   │
│  │  ├─ Routes: auth, user, water-usage                     │   │
│  │  └─ Error Handler: Global error handling                │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Authentication & Authorization                         │   │
│  │  ├─ verifyToken() - JWT validation                      │   │
│  │  └─ requireRole() - Role checking                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  Controllers:                                                     │
│  ├─ authController (signup, login)                              │
│  ├─ userController (profile, users list)                        │
│  └─ waterUsageController (logging, analytics)                   │
│                                                                   │
│  Utilities:                                                       │
│  ├─ tokenUtils (JWT generation)                                 │
│  └─ analyticsUtils (metrics calculation)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │ Mongoose ODM
                             │ Connection Pool
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE (MongoDB)                          │
│              mongodb://localhost:27017/water-dashboard          │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Collections                                             │   │
│  │                                                           │   │
│  │  ┌──────────────────────┐  ┌──────────────────────────┐  │   │
│  │  │  users               │  │  waterusages             │  │   │
│  │  ├──────────────────────┤  ├──────────────────────────┤  │   │
│  │  │ _id (ObjectId)       │  │ _id (ObjectId)           │  │   │
│  │  │ name (String)        │  │ farmer (ref User)        │  │   │
│  │  │ email (String)       │  │ source (String)          │  │   │
│  │  │ password (bcrypt)    │  │ usageAmount (Number)     │  │   │
│  │  │ phone (String)       │  │ date (Date)              │  │   │
│  │  │ location (String)    │  │ cropType (String)        │  │   │
│  │  │ role (farmer/admin)  │  │ areaIrrigated (Number)   │  │   │
│  │  │ farmSize (Number)    │  │ efficiency (Number)      │  │   │
│  │  │ createdAt (Date)     │  │ createdAt (Date)         │  │   │
│  │  │ updatedAt (Date)     │  │ updatedAt (Date)         │  │   │
│  │  └──────────────────────┘  └──────────────────────────┘  │   │
│  │                                                           │   │
│  │  Indexes:                                                │   │
│  │  ├─ email (unique)                                      │   │
│  │  └─ [farmer, date] (for water usage queries)            │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Authentication & Authorization Flow

```
┌─────────────────┐
│  User Signup    │
└────────┬────────┘
         │
         ▼
    ┌─────────────┐       ┌──────────────────┐
    │   Validate  │──────▶│ Check uniqueness │
    └────┬────────┘       │ (email)          │
         │                └──────────────────┘
         ▼
    ┌──────────────┐
    │ Hash password│ (bcryptjs)
    │ with salt:10 │
    └────┬─────────┘
         │
         ▼
    ┌──────────────────┐
    │ Insert User      │
    │ in MongoDB       │
    └────┬─────────────┘
         │
         ▼
    ┌──────────────────┐
    │ Generate JWT     │──────┐
    │ with (id, role)  │      │
    │ expires in 7d    │      │
    └────┬─────────────┘      │
         │                    │
         ▼                    ▼
    ┌─────────────────────────────┐
    │  Response with:             │
    │  - token (JWT)              │
    │  - user data                │
    │  - success message          │
    └─────────────┬───────────────┘
                  │
                  ▼
              ┌──────────────────┐
              │ Frontend:        │
              │ - Store in Redux │
              │ - Save localStorage
              │ - Set auth=true  │
              └──────────────────┘
```

---

## Protected API Call Flow

```
┌──────────────┐
│   Component  │
│   mounted    │
└──────┬───────┘
       │
       ▼
    dispatch(fetchAnalytics())
       │
       ▼
    ┌────────────────────┐
    │  Redux Thunk       │
    │  call API service  │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  Request Interceptor       │
    │  ├─ Get token from Redux   │
    │  └─ Add Authorization      │
    │     header: Bearer <token> │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  Axios HTTP Request        │
    │  GET /api/water-usage/     │
    │      analytics/my          │
    └────────┬───────────────────┘
             │
             ▼ (HTTP)
    ┌────────────────────────────┐
    │  Backend Express Router    │
    │  Route Handler             │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  verifyToken Middleware    │
    │  ├─ Extract JWT            │
    │  ├─ jwt.verify()           │
    │  ├─ Set req.user = decoded │
    │  └─ Next or 401            │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  requireRole Middleware    │
    │  ├─ Check role             │
    │  ├─ Match allowed roles    │
    │  └─ Next or 403            │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  Controller Function       │
    │  waterUsageController.     │
    │  getAnalytics()            │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  MongoDB Query             │
    │  WaterUsage.find({         │
    │    farmer: req.user.id     │
    │  })                        │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  Analytics Calculation     │
    │  calculateAnalytics()      │
    │  ├─ Total usage            │
    │  ├─ Daily/Weekly/Monthly   │
    │  ├─ Generate alerts        │
    │  └─ Generate suggestions   │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  Response JSON             │
    │  {                         │
    │   success: true,           │
    │   data: { metrics... }     │
    │  }                         │
    └────────┬───────────────────┘
             │
             ▼ (HTTP)
    ┌────────────────────────────┐
    │  Response Interceptor      │
    │  ├─ Check status (401?)    │
    │  ├─ Logout if 401          │
    │  └─ Return response        │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  Redux Thunk Fulfilled     │
    │  waterUsageSlice.          │
    │  analytics = payload       │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  useSelector detects       │
    │  state change              │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │  Component re-renders      │
    │  with new data             │
    └────────────────────────────┘
```

---

## Data Model Relationships

```
┌─────────────────────────────────────────────────────┐
│                 users Collection                    │
├─────────────────────────────────────────────────────┤
│  _id (ObjectId)                                     │
│  name                                               │
│  email (unique)                                     │
│  password (bcrypt)                                  │
│  phone                                              │
│  location                                           │
│  role ('farmer' | 'admin')                          │
│  farmSize                                           │
│  state                                              │
│  district                                           │
│  isActive                                           │
│  createdAt                                          │
│  updatedAt                                          │
└────────────────┬────────────────────────────────────┘
                 │ 1
                 │ farmer: ObjectId
                 │ (ref: User)
                 ▼
┌─────────────────────────────────────────────────────┐
│              waterusages Collection                 │
├─────────────────────────────────────────────────────┤
│  _id (ObjectId)                                     │
│  farmer ──┘                                         │
│  source ('borewell' | 'canal' | 'rain' | ...)     │
│  usageAmount (liters)                              │
│  date                                               │
│  cropType (enum)                                    │
│  areaIrrigated (acres)                              │
│  weatherCondition                                   │
│  efficiency (calculated: L/acre)                    │
│  notes                                              │
│  createdAt                                          │
│  updatedAt                                          │
├─────────────────────────────────────────────────────┤
│  Indexes:                                           │
│  - [farmer, date] ← For fast farmer queries         │
│  - [farmer, createdAt] ← For timeline queries       │
└─────────────────────────────────────────────────────┘
```

---

## Component Hierarchy

```
App
├── PublicLayout
│   ├── LoginPage
│   └── SignupPage
│
├── FarmerLayout
│   ├── Header
│   │   ├── User Info Display
│   │   ├── Logout Button
│   │   └── Mobile Menu Toggle
│   │
│   ├── Sidebar
│   │   ├── Dashboard Link
│   │   ├── Water Log Link
│   │   ├── Analytics Link
│   │   └── Profile Link
│   │
│   └── Outlet (Farmer Pages)
│       ├── FarmerDashboard
│       │   ├── StatCard (4 stats)
│       │   ├── Alert Components
│       │   └── Suggestion Cards
│       │
│       ├── WaterLogPage
│       │   ├── Form (8 fields)
│       │   ├── Input Components
│       │   ├── Select Components
│       │   └── Submit Button
│       │
│       ├── AnalyticsPage
│       │   ├── StatCards (3)
│       │   ├── Source Breakdown (Bar)
│       │   ├── Crop Table
│       │   └── Charts
│       │
│       └── ProfilePage
│           └── Edit Form (8 fields)
│
└── AdminLayout
    ├── Header
    ├── Sidebar
    └── Outlet (Admin Pages)
        ├── AdminDashboard
        │   ├── StatCard (4)
        │   ├── Source Distribution
        │   └── Inefficiencies Table
        │
        ├── AdminFarmersPage
        │   └── Farmers Table
        │
        └── AnalyticsPage
            └── (Same as Farmer)
```

---

## Redux Store Structure

```
┌─────────────────────────────────────┐
│          Redux Store                │
├─────────────────────────────────────┤
│                                     │
│  auth: {                            │
│    token: string | null             │
│    user: {                          │
│      id, name, email, role,         │
│      phone, location                │
│    }                                │
│    loading: boolean                 │
│    error: string | null             │
│    isAuthenticated: boolean         │
│  }                                  │
│                                     │
│  user: {                            │
│    user: { profile fields }         │
│    users: [ { user objects } ]      │
│    loading: boolean                 │
│    error: string | null             │
│    pagination: {                    │
│      page, limit, total, pages      │
│    }                                │
│  }                                  │
│                                     │
│  waterUsage: {                      │
│    usages: [ { usage objects } ]    │
│    analytics: {                     │
│      totalUsage,                    │
│      averageDailyUsage,             │
│      dailyUsage,                    │
│      weeklyUsage,                   │
│      monthlyUsage,                  │
│      alerts: [],                    │
│      suggestions: [],               │
│      sourceBreakdown: {},           │
│      cropBreakdown: {}              │
│    }                                │
│    systemAnalytics: { ... }         │
│    loading: boolean                 │
│    error: string | null             │
│    pagination: { ... }              │
│  }                                  │
│                                     │
└─────────────────────────────────────┘
```

---

## API Endpoint Grouping

```
Authentication
├─ POST /api/auth/signup
└─ POST /api/auth/login

User Management
├─ GET /api/users/me
├─ PUT /api/users/me
└─ GET /api/users (Admin)

Water Usage - Farmer
├─ POST /api/water-usage
├─ GET /api/water-usage/my
└─ GET /api/water-usage/analytics/my

Water Usage - Admin
├─ GET /api/water-usage
└─ GET /api/water-usage/analytics/system

Health
└─ GET /api/health
```

---

## Data Flow - Adding Water Usage

```
┌──────────────┐
│  WaterLogPage│ (Component)
└──────┬───────┘
       │ User fills form & clicks submit
       ▼
    ┌────────────────┐
    │ dispatch(      │
    │ addWaterUsage( │
    │ formData)      │
    │ )              │
    └────────┬───────┘
             │
             ▼
    ┌──────────────────────┐
    │  Redux Thunk         │
    │  addWaterUsage.start │
    │  (pending state)     │
    └────────┬─────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │  Axios Request:          │
    │  POST /api/water-usage   │
    │  body: formData          │
    │  header: Authorization   │
    └────────┬─────────────────┘
             │
             ▼ (Network)
    ┌──────────────────────────┐
    │  Backend Express Route   │
    │  verifyToken()           │
    │  requireRole(['farmer']) │
    │  controller.addWaterUsage│
    └────────┬─────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │  MongoDB Create          │
    │  WaterUsage.create({     │
    │   farmer: req.user.id,   │
    │   source,                │
    │   usageAmount,           │
    │   date,                  │
    │   cropType,              │
    │   areaIrrigated,         │
    │   efficiency (calc)      │
    │  })                      │
    └────────┬─────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │  Response: {             │
    │   success: true,         │
    │   data: { ...usage }     │
    │  }                       │
    └────────┬─────────────────┘
             │
             ▼ (Network)
    ┌──────────────────────────┐
    │  Redux Thunk Fulfilled   │
    │  waterUsageSlice:        │
    │  - usages.unshift(new)   │
    │  - loading: false        │
    │  - error: null           │
    └────────┬─────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │  Component Re-renders    │
    │  - Success message shown │
    │  - Form reset            │
    │  - List updated          │
    └──────────────────────────┘
```

---

## Role-Based Access Control

```
        ┌─────────────────────┐
        │   User Attempts     │
        │   Protected Route   │
        └─────────┬───────────┘
                  │
                  ▼
        ┌─────────────────────┐
        │ ProtectedRoute      │
        │ Check auth token    │
        └──────┬──────────────┘
               │
        ┌──────▼──────┐
        │ Is logged in?
        └──┬────────┬─┘
        NO │        │ YES
           │        ▼
           │     ┌──────────────┐
           │     │ Check role   │
           │     └──┬────────┬──┘
           │        │        │
           │       NO       YES (matches)
           │        │        │
           │        ▼        ▼
           │    ┌────────────────────┐
           │    │ Access Denied      │
           │    │ Redirect: /login   │
           │    └────────────────────┘  Render Page
           │                              & Content
           ▼
        ┌────────────────────┐
        │ /login Page        │
        └────────────────────┘
```

---

## Security Layers

```
                    HTTP Request
                         │
                         ▼
        ┌────────────────────────────┐
        │  CORS Middleware           │
        │  (Allow frontend origin)   │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  Body Parser Middleware    │
        │  (Parse JSON)              │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  Route Matching            │
        │  (Find correct handler)    │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  Auth Middleware           │
        │  ├─ Extract JWT            │
        │  ├─ Verify Signature       │
        │  └─ Check Expiry           │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  Authorization Middleware  │
        │  └─ Check User Role        │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  Input Validation          │
        │  ├─ Type checking          │
        │  ├─ Format validation      │
        │  └─ Enum validation        │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  Controller Logic          │
        │  (Process request)         │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  MongoDB Query             │
        │  (Mongoose validation)     │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  Response Generation       │
        │  (JSON response)           │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │  Error Handler Middleware  │
        │  (If any error occurred)   │
        └────────┬───────────────────┘
                 │
                 ▼
                Response
```

---

## Technology Stack Interaction

```
┌──────────────────┐           ┌────────────────┐
│                  │   REST    │                │
│   React App      │◄────────►│ Express Server │
│  (Frontend)      │   JSON    │  (Backend)     │
│                  │           │                │
└──────────────────┘           └────────┬───────┘
        │                              │
        │ Redux State                  │ Mongoose ODM
        │ (Client-side)                │
        │                              ▼
        │                      ┌──────────────────┐
        │                      │  MongoDB         │
        │                      │  (Database)      │
        │                      └──────────────────┘
        │
        ├─ Axios (HTTP client)
        ├─ Tailwind CSS (Styling)
        ├─ React Router (Navigation)
        └─ Redux Toolkit (State Mgmt)

Backend Technology:
├─ Node.js (Runtime)
├─ Express (Web Framework)
├─ Mongoose (ODM)
├─ JWT (Authentication)
├─ bcryptjs (Password Hashing)
├─ Morgan (Logging)
└─ CORS (Cross-Origin)
```

---

This diagram provides a complete visual overview of the system architecture, data flows, and component relationships.
